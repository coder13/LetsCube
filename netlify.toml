[build]
  command = "yarn install --frozen-lockfile --production=false && yarn workspace letscube-client build"
  publish = "client/build"

[build.environment]
  NODE_VERSION = "22.17.0"
  YARN_VERSION = "1.22.22"

[context.deploy-preview]
  ignore = "exit 0"
