{
  "private": true,
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "cypress": "^15.18.1",
    "husky": "4.2.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "cd client/ && yarn lint && cd ../server/ && yarn lint && cd ../client/ && CI=true yarn test"
    }
  }
}
